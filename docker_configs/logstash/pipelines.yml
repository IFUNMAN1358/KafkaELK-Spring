- pipeline.id: log_service
  pipeline.workers: 2
  pipeline.batch.size: 50
  path.config: "/usr/share/logstash/clients/log_service.conf"

- pipeline.id: corporate_messenger_service
  pipeline.workers: 2
  pipeline.batch.size: 50
  path.config: "/usr/share/logstash/clients/corporate_messenger_service.conf"

- pipeline.id: default_service
  pipeline.workers: 2
  pipeline.batch.size: 50
  path.config: "/usr/share/logstash/clients/default_service.conf"